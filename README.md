Europes league website
=================================

### Running

- `yarn install`
- `yarn start`
- go to `localhost:8080`

### Linter settings

Plain [standardjs](https://standardjs.com/)

- `yarn run lint`: checks code (`standard`)
- `yarn run lint:fix`: checks and tries to fix (`standard --fix`)
